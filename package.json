{
  "name": "spotify-play-list",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build --turbopack",
    "start": "next start",
    "lint": "biome check",
    "format": "biome format --write",
    "https": "next dev --experimental-https --hostname 0.0.0.0",
    "dev:local": "NEXT_HTTPS=true NEXT_HTTPS_KEY=certificates/localhost-key.pem NEXT_HTTPS_CERT=certificates/localhost.pem next dev --experimental-https --hostname myapp.local"
  },
  "dependencies": {
    "next": "^16.0.0",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "tsyringe": "^4.7.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
